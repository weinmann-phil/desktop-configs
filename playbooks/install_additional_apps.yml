# Install applications and resources without git or package managers
- name: Install OH-MY-ZSH
  ansible.builtin.script: 'sh -c "$(curl -fsSL https://raw.githubusercontent.com/ohmyzsh/ohmyzsh/master/tools/install.sh)"'
  
